# EasyWine

A GUI for [Wine](https://www.winehq.org/) on macOS that lets you easily control and create new Wine configurations and their settings, powered by Electron.

## Getting started

```bash
npm install     # install dependencies
npm run dev     # launch in development with hot reload
npm run build   # build for production
npm start       # preview a production build
```
